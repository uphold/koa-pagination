{
  "name": "koa-pagination",
  "version": "3.0.0",
  "description": "A middleware to handle Range Pagination Headers using Range & Content-Range entity-headers",
  "license": "MIT",
  "author": "Uphold",
  "main": "src",
  "repository": "uphold/koa-pagination",
  "scripts": {
    "changelog": "github-changelog-generator --future-release=$npm_package_version > CHANGELOG.md",
    "lint": "eslint src test",
    "lint-staged": "lint-staged",
    "test": "jest --coverage --verbose --env=node",
    "version": "npm run changelog && git add -A CHANGELOG.md src"
  },
  "dependencies": {
    "http-content-range-format": "^1.0.0",
    "range-specifier-parser": "^1.0.2",
    "standard-http-error": "^2.0.1"
  },
  "devDependencies": {
    "@uphold/github-changelog-generator": "0.8.0",
    "eslint": "^5.11.1",
    "eslint-config-uphold": "^0.2.0",
    "jest": "^19.0.2",
    "koa": "^2.6.2",
    "lint-staged": "^3.6.1",
    "pre-commit": "^1.2.2",
    "supertest": "^3.3.0"
  },
  "engines": {
    "node": ">=8"
  },
  "lint-staged": {
    "*.js": "eslint"
  },
  "pre-commit": [
    "lint-staged"
  ]
}
